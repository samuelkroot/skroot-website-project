import { useState } from 'react';

const PostForm = () => {
    const [data, setData] = useState({ user: '' });

    return (
        <form>
            <></>
        </form>
    );
};

export default PostForm;
